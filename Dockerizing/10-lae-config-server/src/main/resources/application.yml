server:
  port: 8888
spring:
  application:
    name: lae-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/luciano-espindula/lae-config-server
          #username: username
          #password: password
          default-label: main
          search-paths:
          - 'greeting-service*'